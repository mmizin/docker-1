# --name - flag set the name of the container
# -p  local-machine-port:internal-container-port
# -d - flag to run container in the background
docker run --name overlord --restart always -p 5000:80 -d nginx
